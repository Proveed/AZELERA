<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- 1. CONTENEDOR DE VIDEO -->
    <VideoView
        android:id="@+id/videoPlayer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- 2. CONTENEDOR DE IMAGEN -->
    <ImageView
        android:id="@+id/imgPlayer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone" />

    <!-- 3. CONTENEDOR DE TEXTO (Estilo Marquesina/Cartel) -->
    <LinearLayout
        android:id="@+id/textContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="50dp"
        android:visibility="gone">
        <TextView
            android:id="@+id/txtAd"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="60sp"
            android:textStyle="bold"
            android:textAlignment="center" />
    </LinearLayout>

    <!-- PANTALLA DE BIENVENIDA -->
    <LinearLayout
        android:id="@+id/layoutWelcome"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:background="#0F172A">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AZELERA AD-NETWORK"
            android:textColor="#3B82F6"
            android:textSize="45sp"
            android:textStyle="bold" />
        <Button
            android:id="@+id/btnActivar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"
            android:text="ACTIVAR PANTALLA"
            android:backgroundTint="#2563EB"
            android:padding="20dp" />
    </LinearLayout>

    <!-- CUENTA REGRESIVA UNIVERSAL -->
    <TextView
        android:id="@+id/uiCountdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="20dp"
        android:background="#B0000000"
        android:padding="10dp"
        android:textColor="#FFFFFF"
        android:visibility="gone" />

</RelativeLayout>
